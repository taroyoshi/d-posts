<% if current_user != @user %>
    <div id="follow_form">
        <%# 現ログインユーザが対象のユーザをフォローしているか%>
        <% if current_user.following?(@user) %> 
            <%= render 'unfollow' %>
        <% else %>
            <%= render 'follow' %>
        <% end %>
    </div>
<% end %>